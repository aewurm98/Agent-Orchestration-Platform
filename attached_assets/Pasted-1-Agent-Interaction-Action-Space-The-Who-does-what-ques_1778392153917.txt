1. Agent Interaction & Action Space (The "Who does what?" questions)The text defines the agents' parameters (temperature, max tokens, roles), but not what they actually do in the simulation.What is the exact action space for an individual agent? Are they outputting raw text, structured JSON, or executing explicit tool calls/functions?How does communication flow across the graph edges ($E^{(t)}$)? If Agent A has a directed edge to Agent B, does A's output get appended to B's context window? Is there a central message bus?For the manufacturing domain specifically: How do the distinct roles (Procurement, Operations, Engineering, Sales, Management) interact with the "machines"? Do operations agents issue commands to machines (like "set speed to HIGH"), or are they just passing messages to other agents based on machine readouts?2. The GRPO Advantage & Pruning (The "Credit Assignment" questions)We know low-scoring edges are pruned, but we don't know how the scores are generated.What is the exact formula or mechanism for calculating the advantage score $A_{u \to v}$? How does the system figure out which specific conversation pathway contributed to the overall success or failure of the generation?Is edge pruning permanent? If an edge drops below the $0.3$ threshold and is removed, is there any mechanism to "re-grow" or add new edges later, or does the network only ever shrink?3. Environment & Fitness Mechanics (The "How is it measured?" questions)The fitness function is $f = \frac{s}{\lambda \cdot c}$, but the units matter.How are latency ($\lambda$) and cost ($c$) quantified? Is latency measured in wall-clock time (seconds) or generation steps? Is cost measured in API dollars, total token count, or simulated currency?What happens during an "environment tick"? Is this a synchronous turn-based system (everyone speaks once per tick), or is it an asynchronous event-driven loop?4. Evolutionary Algorithm Quirks (The "Why is it built this way?" questions)There are a couple of strange design choices in the algorithm that seem like placeholders or shortcuts.Why is the Confidence Score ($\kappa$) a random number? The text says $\kappa^{(t)} \sim \text{Uniform}(0.3, 0.95)$. This means the Human-In-The-Loop (HITL) gate triggers entirely randomly (whenever the dice roll is under 0.6), rather than being based on actual model uncertainty. Is this a placeholder for a future uncertainty-estimation module?Why is there no elitism? The summary explicitly states "the child always replaces the parent regardless of fitness comparison." Why allow the system to step backward into a worse configuration instead of keeping the best-known state?